import java.util.Comparator;

/**
 * Comparator used to order Visitor objects according to
 * multiple comparison criteria.
 */
public class VisitorComparator implements Comparator<Visitor> {

    @Override
    public int compare(Visitor o1, Visitor o2) {
        // Comparison logic will be implemented in later commits.
        return 0;
    }
}
