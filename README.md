# username-A2

PROG2004 / Object Oriented Programming – Assignment Two

This repository contains the source code, tests and documentation for Assignment Two.

## Structure

- `src/` – main Java source files
- `test/` – basic test files (optional but recommended)
- `data/` – sample CSV data and exported ride history
- `docs/` – design notes, testing strategy, user manual, video script and UML diagrams
- `.vscode/`, `.idea/` – IDE configuration (optional)
- `lib/` – external libraries (if any)

## Build & Run

The project can be compiled and run using a standard Java compiler or an IDE.
Detailed instructions will be added later.
